angular.module('ChessScouterApp')
    .factory('forks', function() {
        return function(game, x, y) {
            // return a list of forks for the piece located at (x, y) in the game
        }
    });