angular.module('ChessScouterApp')
    .factory('getLegalMoves', function() {
        return function(game, x, y) {
            // return a list of legal moves for the piece located at (x, y) in the game
        }
    });