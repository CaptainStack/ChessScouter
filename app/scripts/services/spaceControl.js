angular.module('ChessScouterApp')
    .factory('spaceControl', function() {
        return function(game, x, y) {
            // return the number of pieces from each player that controls square (x, y)
        }
    });