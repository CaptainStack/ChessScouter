angular.module('ChessScouterApp')
    .factory('pieceFlair', function() {
        return function(game, x, y) {
            // return a list of pieces attacked by the piece located at (x, y) in the game
        }
    });