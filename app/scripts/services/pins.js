angular.module('ChessScouterApp')
    .factory('pins', function() {
        return function(game, x, y) {
            // return a list of pins for the piece located at (x, y) in the game
        }
    });